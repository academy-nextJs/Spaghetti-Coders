import { Button } from "@heroui/react";

export default function SendBtn() {
    return (
        <Button className="rounded-full w-[390px] text-sm bg-[#7575FE] h-8 text-white flex items-center justify-center">
             ارسال کد تایید 
        </Button>

    );
}