import { Button } from "@heroui/react";

export default function MainPage() {
    return (
        <Button className="rounded-full bg-[#F0F0F0] h-[40px] w-[110px] text-black flex items-center justify-center">
            صفحه اصلی 
        </Button>

    );
}