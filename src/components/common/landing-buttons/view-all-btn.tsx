import { Button } from "@heroui/react";

export default function ViewAll() {
    return (
        <Button className="rounded-full text-sm bg-[#7575FE] h-8 text-white flex items-center justify-center">
             مشاهده همه
        </Button>

    );
}