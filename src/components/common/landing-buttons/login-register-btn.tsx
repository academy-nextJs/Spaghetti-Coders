import { Button } from "@heroui/react";

export default function LoginRegisterBtn() {
    return (
        <Button className="rounded-full bg-[#7575FE] h-8 text-white flex items-center justify-center">
            ثبت نام / ورود
        </Button>
        
    );
}